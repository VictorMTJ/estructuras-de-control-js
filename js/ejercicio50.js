// Definir las variables de temperatura y presión
let temperatura = 105; // ejemplo de valor para la temperatura
let presion = 210;     // ejemplo de valor para la presión

// Sentencia if-else para verificar las condiciones
if (presion > 200 || temperatura > 100) {
    console.log("Alarma");
} else {
    console.log("Normal");
}
