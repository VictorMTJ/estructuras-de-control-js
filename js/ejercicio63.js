// Solicitar la edad al usuario
let edad = parseInt(prompt("¿Cuántos años tienes?"));

// Mostrar los años cumplidos desde 1 hasta la edad proporcionada
for (let i = 1; i <= edad; i++) {
    console.log(`Año ${i}`);
}
